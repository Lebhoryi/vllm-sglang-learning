# 07 vllm bench serve 结果解读卡（2026-09-08，按 v0.28 源码实测格式）

> 用于 9/21 检查点②（27B 基线）后解读输出。字段与打印格式均来自 `vllm/benchmarks/serve.py`（main 869f78732b）核实。
> 跑法见 docs/00 Step 4 / `scripts/bench_qwen38_27b.sh`。

## 一、输出长这样（v0.28 实测格式）

```
============== Serving Benchmark Result ==============
Successful requests:                      200
Failed requests:                            0
Maximum request concurrency:                16
Benchmark duration (s):                   xx.xx
Total input tokens:                    102400
Total generated tokens:                  25600
Request throughput (req/s):              xx.xx
Output token throughput (tok/s):         xx.xx
Peak output token throughput (tok/s):    xx.xx
Peak concurrent requests:                xx
Total token throughput (tok/s):          xx.xx
---------------- TTFT (Time to First Token) ----------------
Mean TTFT (ms):       ...
Median TTFT (ms):     ...
P50/P90/P95/P99 TTFT (ms): ...
---------------- TPOT (Time per Output Token, excl 1st) ----
...
---------------- ITL (Inter-token Latency) ----
...
---------------- E2EL (End-to-end Latency) ----
...
```

> 百分位默认取 `--percentile-metrics` 指定的集合（默认含 p50/p90/p95/p99，`--metric-percentiles` 可改）。
> `--save-result` 会把完整字段写入 json（含每请求的 ttfts/itls/output_lens/generated_texts/errors 等），比终端打印更全，**必须存档**。

## 二、指标含义与解读要点

| 指标 | 含义 | 解读注意 |
|---|---|---|
| Successful / Failed | 成功/失败请求数 | **先看这个**：有 failed 就找 `errors` 字段，别急着聊吞吐 |
| Request throughput (req/s) | 每秒完成请求数 | 受 `--request-rate inf`（打满）与 `--max-concurrency` 影响 |
| Output token throughput (tok/s) | 每秒输出 token 数（不含输入） | **服务商报价/自研栈对比最常用** |
| Total token throughput | 输入+输出总吞吐 | 负载长度不同不可直接比 |
| Peak concurrent requests | 观测到的最大并发 | 与 `--max-concurrency` 对比：远小于它 = 瓶颈在生成速度 |
| TTFT（均值/中位/P99） | 首 token 延迟 | 影响"体感"；**P99 比均值重要**（长尾） |
| TPOT（均值/中位/P99） | 每个输出 token 的间隔（不含首个） | 与"token/s 手感"互推（1/TPOT） |
| ITL | 相邻 token 间隔（含首 token 到第二个） | 流式体验指标，一般略高/近于 TPOT |
| E2EL | 端到端延迟（整个请求） | 与输出长度强相关，跨负载别直接比 |
| RTFx | Inverse Real-Time Factor | >1 表示比实时快；v0.28 新指标（880 行左右算的，读 `rtfx` 字段） |

## 三、Sanity check（数对不对）清单

1. **failed == 0**（有错误先看 json 的 `errors`）
2. **输出长度达标**：`--random-output-len 128` → 平均实际输出长度应接近 128（过短 = 被 EOS/stop 截断，过长 = 忽略 EOS）
3. **吞吐量纲自洽**：request_throughput × (input+output)/req ≈ total_token_throughput
4. **TPOT 与吞吐互推**：单并发下 1/TPOT ≈ 每请求 token/s；多并发下吞吐 ≈ 并发数/（prefill+decode 混合成本）
5. **基线可比性**：对比时必须同 input-len/output-len/concurrency/request-rate——否则数字没有意义

## 四、存档模板（9/21 填）

```markdown
## 27B vLLM 基线（2026-09-21）
- 环境：主力实验机A100-80G；vllm 0.28.0；`--max-model-len 16384 --max-num-seqs 16 --gpu-memory-utilization 0.9`
- 负载：random 512 in / 128 out，200 prompts，concurrency 16，rate=inf
- 结果：req/s=__；output tok/s=__；TTFT p50/p99=__/__ms；TPOT p50/p99=__/__ms
- json：logs/bench-27b-vllm-*.json
- 备注：（CUDA graph 生效？KV 利用率？）
```

## 五、给自研栈（Lyngor）的对照口径

- 同负载、同并发、同长度下，`output token throughput` 与 `TTFT p99` 是跨栈对比的核心指标
- vLLM 的服务端指标在 `/metrics`（Prometheus，含 `vllm:num_requests_running` 等），benchmark 之外可留一手