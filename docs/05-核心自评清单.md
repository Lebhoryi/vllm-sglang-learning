# 05 核心自评清单（10/08 收尾用，2026-09-08 预建）

> **用法**：10/08 前逐项自评。每项三档：**能讲清**（给别人讲 5 分钟）/ **能画出**（白板画图）/ **能复现**（自己写码/算数能对上）。
> 全部 ✓ = 一个月目标达成。对应材料列在每项后面。

## 1. 请求生命周期（docs/01 + 01B）

- [ ] 能画出 vLLM 全链路图（entrypoint → AsyncLLM → EngineCoreClient → EngineCore.step → scheduler → executor → output）
- [ ] 能讲清每跳传的对象（EngineCoreRequest / SchedulerOutput / EngineCoreOutputs / RequestOutput）
- [ ] 知道离线 LLM API（InprocClient）与 serve（AsyncMPClient 独立进程）的通信差异
- [ ] 能讲清 preemption 主路径 = recompute（v0.28 无 CPU swap）
- [ ] 知道 detokenizer 是增量式的（流式如何实现）

## 2. 调度与批处理（docs/02 专题 1）

- [ ] 能讲清 continuous batching 的一次 step 编排
- [ ] 知道 chunked prefill 默认开、chunk 上限 = max_num_batched_tokens（默认 2048）
- [ ] 知道 prefix caching 默认开；命中判定在 KVCacheManager.get_computed_blocks
- [ ] 能讲清 preemption 触发条件与 victim 选择

## 3. KV cache 与显存（docs/02 专题 2）

- [ ] **能复现**：用 config.json + layout 算出日志里的 67.08 GiB / 5,788,118 tokens（0.8B）与 18.61 GiB / 298,275 tokens（27B）
- [ ] 能讲清 block table（PagedAttention）与 block_pool 的关系
- [ ] 能讲清 gpu_memory_utilization / max_num_seqs / max_model_len 三者互咬
- [ ] 能讲清 MLA 低秩 KV 为什么省显存（为 V4 铺垫）

## 4. 注意力后端与 kernel（docs/02 专题 3）

- [ ] 能画出 Qwen3.5 层结构图（GDN 线性注意力 + Gated Attention 的交替排列）
- [ ] 能讲清 GDN 与标准 attention 的 kernel 差异
- [ ] 知道 selector 双后端并行（get_attn_backend + get_mamba_attn_backend）
- [ ] 能讲清 attention page 与 mamba page 对齐（你日志里的 784 block size）

## 5. 性能标杆方法论（docs/00 Step 4）

- [ ] 会用 `vllm bench serve`（v0.28 新 CLI）并解释各参数
- [ ] 能解读 TTFT / TPOT / 吞吐，能复述 27B 基线数字
- [ ] 知道 262K 上下文下 27B 并发只有 1.14x 的原因（KV 容量账）

## 6. SGLang 对照（docs/03，10/01 后）

- [ ] 对照矩阵填完（≥7 行）
- [ ] 能讲 3 条 SGLang 与 vLLM 的设计差异（Radix / schedule policy / 后端选择）
- [ ] 能说出 deepseek_v4_backend 与 vLLM mla 稀疏后端的对应关系

## 7. V4 概念映射（docs/04）

- [ ] 映射表「学习要点」列填满
- [ ] 能画出 V4 结构骨架（CSA/HCA/MQA-MLA/MoE 装配）
- [ ] 知道 xpu/ 双后端 = 自研栈对照模板
- [ ] 写出 ≥3 条「对自研栈的启示」（Lyngor 视角）

## 8. 十月 issue 候选池（P4 前置）

- [ ] 攒了 ≥3 个候选（阶段一/二遇到的报错、行为差异、源码疑问）
- [ ] 每个候选有：现象 / 复现步骤 / 初步定位

## 检查点回顾

| 检查点 | 目标 | 结果 |
|---|---|---|
| 09/14 | 0.8B HTTP+curl / 生命周期图 / 6 问自答 | ☐ |
| 09/21 | 27B bench 基线（TTFT/TPOT/吞吐） | ☐ |
| 09/30 | docs/02 三专题完成（硬） | ☐ |
| 10/08 | 本清单全勾 | ☐ |