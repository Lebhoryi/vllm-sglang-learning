# 进度日志（vLLM/SGLang 月度学习）

## 2026-09-11

- ✅ **方法决策（用户拍板）**：采纳"测试驱动深读"——vllm/tests 不单独插队，作为阶段三（docs/02）深读的"第一站"工具
- 📄 agent 侧：`docs/09-测试地图.md` 建成——tests/ 全库盘点（1518 文件，v1/ 368 最大）+ 三专题 × 测试文件索引 + 工作流（读断言→读源码→跑/改参）+ 测试套路速查 + 实操建议
- 🎯 **尝鲜深读成果**：`tests/v1/core/test_mamba_align_chunk_split.py`（388 行，bug #43559 回归测试）全文精读——不变量（slot p = (p+1)×block state）、12 组测试组织、源码锚点（scheduler.py:404 `_mamba_block_aligned_split`、kv_cache_interface.py:973 `is_mamba_prefill_checkpoint_valid`、platforms/interface.py:934 日志出处）→ 日志 784 因果链闭合（见 docs/09 §2.4）
- 📌 待办（9/14 检查点① 后）：按 docs/09 地图进入阶段三深读——先从跑 `cpu_test` 用例验证入手

## 2026-09-08

- ✅ **vLLM 0.8B 离线 LLM API 跑通**（~14:00）：`test_vllms/test_vllm_qwen3.5_0.8b.py` → `test_qwen3.5_0.8b.log`
  - 模型：`<model_dir>/Qwen3.5-0.8B`；GDN kernel 预热；KV 67.08 GiB / 5,788,118 tokens / 22.08x@262K；CUDA graph 4s
- ✅ **vLLM 27B 离线 LLM API 跑通**（15:25）：`test_vllms/test_vllm_qwen3.8_27b.py` → `test_qwen3.8_27b.log`
  - 模型：`<model_dir>/Qwen3.8-27B`；模型加载 51.1 GiB（11.4s）；KV 18.61 GiB ≈ **298,275 tokens，262K 上下文并发仅 1.14x** → 限 `max-model-len` 的必要性实锤
  - 金矿：`Setting attention block size to 784 tokens ... >= mamba page size`（混合注意力 page 对齐，docs/02 专题 3 实证）
- ✅ **vLLM 0.8B HTTP serve + curl 冒烟通过**（09-09 08:36）：`./scripts/start_vllm_qwen35_08b.sh` + `./scripts/test_chat.sh`（`set_env.sh` 自动加载 MODEL_DIR）→ OpenAI 兼容端点正常回复中文，`finish_reason=stop`，usage 18+28 tokens → **9/14 检查点① 的 serve+curl 环节完成**；剩：生命周期图 + 6 问自答
- 🚧 待办（9/14 检查点①）：`vllm serve` HTTP 模式 + curl；docs/01 生命周期走读 + 图 + 6 问
- 🚧 待办（9/21 检查点②）：27B `vllm bench serve` 基线（v0.28 新 CLI，docs/00 Step 4）
- 🏗️ agent 侧：docs/04 V4 映射骨架已预建（V4 实现位置实测：vllm models/deepseek_v4/{nvidia,xpu} + mla 稀疏后端族；sglang deepseek_v4*.py + deepseek_v4_memory_pool + hybrid_cache）
- 🏗️ agent 侧：docs/01B-参考答案.md（6 通关问题答案 + 源码行号证据 + 新版认知点）；scripts/start_vllm_qwen38_27b.sh + scripts/bench_qwen38_27b.sh（27B serve/bench 一键脚本）
- 🏗️ agent 侧：docs/02 附录 A（配置默认值：chunked prefill/prefix caching v0.28 均**默认开**、max_num_batched_tokens=2048、serve 参数入口）；docs/05-核心自评清单（10/08 收尾自评 8 模块）
- 🏗️ agent 侧：docs/02 附录 B（0.8B 结构档案：config.json 实测 = [lin×3+full×1]×6、GQA 2KV头/head_dim 256、GDN 16头/128dim/fp32；KV 显存推导框架 + 3 个引导问题：12.4KB/token 的矛盾点）
- 🏗️ agent 侧：docs/02 附录 B2（27B 档案：64 层、GQA 4头、linear value 48头；KV 比值 5.28 vs full 部分 5.33 的"巧合"提示）；docs/06-issue候选池（模板 + 3 条弱信号登记）
- 🏗️ agent 侧：docs/04 附录（vLLM 侧 V4 结构实测：MegaMoE/FP8FP4/SM100、DeepseekV4Attention.forward_mqa + C4A indexer、三稀疏后端、EP、DSpark/MTP 位置，精确到 file:line）
- 🏗️ agent 侧：docs/07-基准解读卡（v0.28 bench serve 输出格式实测 + 指标解读 + sanity check + 9/21 存档模板 + Lyngor 对照口径）
- 🏗️ agent 侧：docs/02 附录 C（混合注意力 serving flags：mamba-cache-mode none/all/align 语义 + Qwen3.5 禁 all + 与日志 784 的因果链）；scripts/start_vllm_qwen25_05b.sh（0.5B 对照模型脚本）
- 🏗️ agent 侧：docs/08-学习材料索引（vllm-learning-book 64 章 ↔ 三阶段映射 + 版本差异警告：书锁 b23bd73f 旧于本地源码 1463 commits）
- 🏗️ agent 侧：docs/04 附录 2（SGLang 侧 V4 结构预核实：MQALayer/C4Indexer/Compressor/FP8 dsv4/昇腾路径；亮点 = vLLM FP4 vs SGLang FP8 indexer 量化位点差异，自研栈必拍板点）
- 🔧 修复：scripts/*.sh 全部补执行位（原为 -rw-------，直接 ./ 运行会 Permission denied）+ bash -n 语法检查 8/8 通过
- 🏗️ agent 侧：docs/01C-生命周期参考图（mermaid + 文本双版本，锚点全部源码实测；与 01B 配套的"先画再对"参考）
- 📝 用户侧：自建 week1-4 周目录并自写周 README（正确消化 docs/00；week2 命令引用 `vllm bench serve` 无误）；agent 未覆盖其文件
- 📄 agent 侧：`week1-vllm跑通/Week1-学习报告-环境复现与日志分析.md`（背景/三引擎简介/学习地图+当前位置/环境说明/全流程复现命令/test_vllms 双日志逐段分析/test_chat.sh 冒烟三请求分析）
- 🎨 agent 侧：`images/` 5 张信息图（SVG 源 + PNG 2x 导出，cairosvg + Noto Sans CJK）：01-一个月学习地图 / 02-三引擎角色图 / 03-跑通流水线 / 04-显存账对比 / 05-日志彩蛋卡，已嵌入学习报告；暖色卡片风、几何图标（无 emoji），修过 badge 文字溢出与小标签重叠
- 🔧 防坑修复：test_chat.sh 与 docs/00 的 curl model 字段改为本地路径（源码核实 get_served_model_name 默认返回 model 参数本身；原 HF 名会 404）
- 📄 agent 侧：`docs/01D-走读日志对照卡.md`（生命周期 16 跳 ↔ serve/离线双日志证据行 ↔ 实操入口三栏对照；结论：serve 链路走 scripts/，引擎内核走 test_vllms/，两套日志拼起来 = docs/01 全链路）

## 检查点

- [ ] 09/14：vLLM 0.8B HTTP+curl / 生命周期图 / 通关问题
- [ ] 09/21：27B benchmark 基线（TTFT/TPOT/吞吐）
- [ ] 09/30：核心模块深读完成（docs/02 三专题，硬检查点）
- [ ] 10/08：SGLang 对照矩阵 + V4 映射 + 自评